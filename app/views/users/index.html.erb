<h2> Users </h2>

<%= form_tag users_path, :method => 'get' do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
  </p>
<% end %>

<% if @users.any? %>
<%= will_paginate @users%>	 
 <ul class = "reads">
<%= render @users %>
</ul> 
<%= will_paginate @users%>	 
<% end %>